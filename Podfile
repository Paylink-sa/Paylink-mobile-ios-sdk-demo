platform :ios, '13.0' # Replace with the desired minimum iOS version

target 'TestPLPaymentGateway' do
  use_frameworks!
  pod 'PaylinkGateway', :git => 'https://github.com/Paylink-sa/PLPaymentGatewayPod.git'
  # Other dependencies for your project
end
